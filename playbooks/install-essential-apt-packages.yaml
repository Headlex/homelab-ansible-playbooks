---
- hosts: all
  become: yes
  tasks:
    - name: Update apt cache and make sure gpg, fish and neofetch are installed
      apt:
        name: "{{ item }}"
        update_cache: yes
      loop:
        - fish
        - neofetch
        - gpg